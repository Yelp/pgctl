dependencies:
    pre:
        - sudo apt-get update
    override: ./CI install
    cache_directories:
        - "~/.pip"
        - "~/.pre-commit"
        - "~/.cache"

test:
    environment:
        TOP: $PWD 
        SCRATCH: $(mktemp -d)
    pwd: $SCRATCH
    override:
        - $TOP/test
